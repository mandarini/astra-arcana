{
  "name": "@astra-arcana/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "nx": {
    "name": "api",
    "targets": {
      "dev": {
        "continuous": true,
        "dependsOn": [
          "cf-typegen"
        ]
      }
    }
  }
}
